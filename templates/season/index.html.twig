{% extends 'base.html.twig' %}

{% block title %}{{ season.name }} - {{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">Mensański Typer</li>
                        <li class="breadcrumb-item" aria-current="page">{{ season.name }}</li>
                        <li class="breadcrumb-item active" aria-current="page">Terminarz</li>
                    </ol>
                </nav>
            </div>
        </div>
        <div class="row">
            {% include 'season/_partials/left_column.html.twig' %}
            <div class="col-lg-9">
                {% block content %}
                    <div class="card">
                        <div class="card-header">
                            {{ season.name }}
                        </div>
                        <div class="card-body">
                            {{ season.slug }}
                        </div>
                    </div>
                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}