{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">Mensański Typer</li>
                        <li class="breadcrumb-item" aria-current="page">{{ 'user.profile'|trans }}</li>
                    </ol>
                </nav>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-3">
                <div class="col-sticky-top">
                    <div class="card">
                        <div class="card-header">
                            {{ 'user.profile'|trans }}
                        </div>
                        <div class="card-body">
                            <a class="card-link" href="{{ path('user_profile') }}">{{ 'card.summary'|trans }}</a><br>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-9">
                {% block content %}
                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}