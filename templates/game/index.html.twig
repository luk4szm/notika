{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">Mensański Typer</li>
                        <li class="breadcrumb-item" aria-current="page">{{ game.season.name }}</li>
                        <li class="breadcrumb-item active" aria-current="page">{{ game.home.name }} vs. {{ game.guest.name }}</li>
                    </ol>
                </nav>
            </div>
        </div>
        <div class="row">
            {% include 'game/_partials/left_column.html.twig' %}
            <div class="col-lg-9">
                {% block content %}
                {% endblock %}
            </div>
        </div>
    </div>

    {% block charts %}
    {% endblock %}
{% endblock %}
