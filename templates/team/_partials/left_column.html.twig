<div class="col-lg-3">
    <div class="col-sticky-top">
        <div class="card">
            <div class="card-header text-center">
                {{ team.name }}
            </div>
            <div class="card-body">
                <a class="" href="{{ path('team_detail', {'slug': team.slug}) }}">{{ 'card.summary'|trans }}</a>
            </div>
        </div>
    </div>
</div>
