{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
    {% if nextGame %}
        {{ nextGame.home.name }} vs. {{ nextGame.guest.name }} <br/>
        {{ nextGame.goalsHome }}:{{ nextGame.goalsGuest }}
    {% else %}
        brak nadchodzących spotkań
    {% endif %}

    {{ dump(missingBets) }}
{% endblock %}
