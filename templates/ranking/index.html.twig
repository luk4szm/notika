{% extends 'base.html.twig' %}

{% block title %}{{ ranking.name }} - {{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">Mensański Typer</li>
                        <li class="breadcrumb-item" aria-current="page">{{ ranking.name }}</li>
                        <li class="breadcrumb-item active" aria-current="page">Klasyfikacja</li>
                    </ol>
                </nav>
            </div>
        </div>
        <div class="row">
            {% include 'ranking/_partials/left_column.html.twig' %}
            <div class="col-lg-9">
                {% block content %}
                    <div class="card">
                        <div class="card-header">
                            {{ ranking.name }}
                        </div>
                        <div class="card-body">
                        </div>
                    </div>
                {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}